'use client'

import styles from './slider.module.scss'

/**
 * Task B — Image Slider (Swiper)
 *
 * One slide at a time, autoplaying every 5s. Show the headline and copy
 * above the slider. Configure the Swiper instance to match the design.
 *
 * Docs: https://swiperjs.com/ (see `swiper/react`).
 * Marked `'use client'` because Swiper relies on browser APIs.
 */
const SliderBlock = () => (
  <section className={`${styles.root} container`}>Slider block</section>
)

export default SliderBlock
